<%= simple_form_for([list, bookmark]) do |f| %>
    <%= f.input :comment %>
    <%= f.association :movie, collection: @movies, input_html: { class: 'select2' } %>
    <%= f.button :submit, class: "btn btn-outline-danger rounded" %>
  <% end %>
